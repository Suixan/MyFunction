/*
#include <stdio.h>
#include <io.h>

int file_exist(char * filename)
{
	return (access(filename, 0) == 0);
}

int main()
{
	printf("Does NOTEXIST.FIL exist: %s\n", file_exist("NOTEXIST.FIL")  ? "YES" : "NO");


	return 0;
}
*/